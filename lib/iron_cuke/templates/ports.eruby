Feature: Checking Ports
  In order to test a chef node
  As a system administrator
  I want to examine the ports of a system
        
  Scenario: The proper ports are open
    Then these ports are open:
      | port |
   <% @ports.each do |port| %>
      | <%= port[:port] -%> |
   <% end %>
